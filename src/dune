;; installed executables / public targets
(library
  (name aricomp)
  (public_name aricomp)
  (modules AC)
  (libraries batteries dolog line_oriented unix zarith))

(executables
  (names test)
  (modules test)
  (libraries aricomp))
